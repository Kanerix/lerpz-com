{
  "name": "lerpz-ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:portal": "pnpm --filter portal dev",
    "build:portal": "pnpm --filter portal build",
    "start:portal": "pnpm --filter portal start",
    "lint:portal": "pnpm --filter portal lint",
    "format:portal": "pnpm --filter portal format",
    "generate:api:portal": "pnpm --filter portal generate:api"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.0.6",
  "devDependencies": {
    "@biomejs/biome": "2.4.14",
    "@lerpz/typescript-config": "workspace:*"
  }
}
