// The company site is fully static: every route is prerendered to HTML at
// build time and served by a plain file server, with no request-time rendering.
export const prerender = true;
export const ssr = true;
