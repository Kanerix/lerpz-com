name: "Detect changed services"

on:
  workflow_call:
    outputs:
      coruscant:
        value: ${{ jobs.detect-changes.outputs.coruscant }}
      kamino:
        value: ${{ jobs.detect-changes.outputs.kamino }}
      artoo:
        value: ${{ jobs.detect-changes.outputs.artoo }}

jobs:
  detect-changes:
    name: "Detect changed services"
    runs-on: ubuntu-latest

    outputs:
      coruscant: ${{ steps.filter.outputs.coruscant }}
      kamino: ${{ steps.filter.outputs.kamino }}
      artoo: ${{ steps.filter.outputs.artoo }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Detect changed paths per service
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            coruscant:
              - 'svc/coruscant/**'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'pnpm-workspace.yaml'
              - 'tsconfig.json'
            kamino:
              - 'svc/kamino/**'
              - 'crates/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            artoo:
              - 'svc/artoo/**'
              - 'crates/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
